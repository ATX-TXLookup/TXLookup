node_modules/
.next/
.env
__pycache__/
*.pyc
.venv/
dist/
.vercel/
.DS_Store
*.tsbuildinfo

# Per-worktree agent log (see AGENTS.md "Logging convention")
log.md

# Local run archive (issue #59 fallback) — never commit captured queries.
data/runs/
.env.local
.env.*.local
.vercel
.env*.local

# Local-only team contact info (emails are PII — never commit)
docs/team-contacts.local.md

# Local-only working notes (partner feedback, drafts)
notes/
